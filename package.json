{
  "name": "pbl-presentation-v2",
  "private": true,
  "scripts": {
    "dev": "slidev --open",
    "build:slides": "slidev build --out dist/slides --base /slides/",
    "build": "rm -rf dist && npm run build:slides && cp index.html sdc_logo.jpg dist/ && cp sdc_logo.jpg dist/slides/",
    "export": "slidev export"
  },
  "dependencies": {
    "@slidev/cli": "^0.48.0",
    "slidev-theme-dracula": "latest",
    "playwright-chromium": "^1.41.0"
  }
}
